# toy-model-superposition

This project explores a minimal reproduction of the paper [*Toy Models of Superposition*](https://arxiv.org/abs/2209.10652) using `jax`.


The `notebooks` directory contains demonstrations of the code with visualisations.

## Setup

```bash
uv sync
source .venv/bin/activate
```